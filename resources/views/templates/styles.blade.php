
   @switch($route)
       @case('login')
            <link href="{{ asset('css/auth/styles.css') }}" rel="stylesheet">
           @break
       @case(2)
           
           @break
       @default
           
   @endswitch